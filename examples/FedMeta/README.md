# Federate Meta Learning by FLamingo framework

## Usage
Fillin config.yaml and run. Remember to generate dataset first.

The `sys.path.append` is not necessary if you have installed FLamingo.

```bash
python main.py
```